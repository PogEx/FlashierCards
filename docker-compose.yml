services:
  backend.restapi:
    image: ghcr.io/pogex/flashiercards-backend.restapi:develop
    restart: unless-stopped
    ports:
      - "8080:8080"
      - "8081:8081"
    environment:
      ASPNETCORE_ENVIRONMENT: "Development"
    networks:
      - "frontend"
      - "database"
    volumes:
      - rest-backend-data-store:/flashiercards/data
      - rest-backend-config:/flashiercards/config
    depends_on:
      - db

  backend.html:
    image: ghcr.io/pogex/flashiercards-backend.html:develop
    restart: unless-stopped
    ports:
      - "8082:8080"
      - "8083:8081"
    environment:
      ASPNETCORE_ENVIRONMENT: "Development"
    networks:
      - "frontend"
    volumes:
      - html-backend-config:/flashiercards/config
    depends_on:
      - backend.restapi
    
  db:
    image: mysql:latest
    restart: unless-stopped
    stdin_open: true
    tty: true
    environment:
      MYSQL_ROOT_PASSWORD: ${DB_PASSWORD}
      MYSQL_DATABASE: flashiercards
    ports:
      - "8079:3306"
      - "22222:22"
    networks:
      - "database"
    volumes:
      - db-data:/etc/data
      - db-config:/var/lib/mysql
  
networks:
  frontend: {}
  database: {}
  
volumes:
  db-data:
  db-config:
  rest-backend-data-store:
  rest-backend-config:
  html-backend-config:
  
